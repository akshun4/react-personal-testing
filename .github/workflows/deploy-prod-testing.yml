name: Deploy Docker Image in Prod

on:
  push:
    branches:
      - 'release-v**'
      - 'release/v**'

jobs:
  build-and-deploy-to-acr:
    runs-on: ubuntu-latest
    environment:
      name: prod
    name: Build and Deploy API Image to ACR
    steps:     
      - uses: actions/checkout@v3

      - name: Fetch release version from release name
        run: |
          echo "ENVIRONMENT_VERSION=${{ GITHUB_REF_NAME }}" | tr -d 'a-zA-Z_=-/' >> $GITHUB_ENV;
      
      - name: Set up Node.js version
        uses: actions/setup-node@v2
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'yarn'